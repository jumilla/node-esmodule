{
    "private": true,
    "workspaces": [
        "packages/*",
        "fixtures/*",
        "examples/*"
    ],
    "name": "node-esmodule",
    "devDependencies": {
        "@babel/core": "^7.10",
        "@babel/preset-env": "^7.10",
        "jest": "^26.0",
        "lerna": "^3.22",
        "typescript": "^3.9"
    },
    "scripts": {
        "build": "yarn run tsc -p packages/builder/",
        "test": "jest"
    },
    "dependencies": {
        "@types/babel__preset-env": "^7.9.0"
    }
}
